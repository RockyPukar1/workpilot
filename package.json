{
  "name": "workpilot-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "WorkPilot - Corporate Productivity Suite Chrome Extension",
  "scripts": {
    "dev": "pnpm --filter extension dev",
    "build": "pnpm --filter extension build",
    "build:backend": "pnpm --filter backend build",
    "dev:backend": "pnpm --filter backend dev",
    "install:all": "pnpm install && pnpm --filter extension install && pnpm --filter backend install"
  },
  "keywords": ["chrome-extension", "productivity", "corporate"],
  "author": "",
  "license": "MIT"
}

